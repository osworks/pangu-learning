spring:
  application:
    name: pangu-discovery-http-provider
  profiles:
    active: ${env:dev}
  cloud:
    nacos: 
      config:
          server-addr: ${nacos:localhost:8848}
          file-extension: yml