spring:
  application:
    name: pangu-external-config
  profiles:
    active: ${env:dev}
  cloud:
    nacos: 
      config:
          server-addr: ${nacos:localhost:8848}
          file-extension: yml